pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = 'NeoStrike2D'

include ':app'
include ':capacitor-cordova-android-plugins'

apply from: 'capacitor.settings.gradle'
